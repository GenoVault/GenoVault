import { defineConfig } from 'vitest/config'

/**
 * Таймаут тесту — 30 секунд замість типових 5.
 *
 * У цьому пакеті живуть властивісні тести розподілу й квоти: кожен ганяє 500
 * випадкових пулів по 50 датасетів, і наодинці це ~0,5 секунди. Але `pnpm gate`
 * запускає вісім пакетів паралельно, а на цій машині поверх гейту зазвичай ще
 * підняті локальний валідатор і шість контейнерів Arcium — і тоді ті самі 500
 * ітерацій виходять за п'ять секунд.
 *
 * Падіння за таймаутом виглядає як зламана арифметика розподілу («сума не
 * сходиться»), хоч означає лише зайнятий процесор, — і саме тому запас тут
 * шістдесятикратний, а не подвійний. Тест, який залежить від навантаження
 * машини, не перевіряє нічого: він однаково червоніє і тоді, коли все добре.
 */
export default defineConfig({
  test: {
    testTimeout: 30_000,
  },
})
